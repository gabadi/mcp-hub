package types

const (
	// Layout constants
	COLUMN_WIDTH = 28 // Fixed width for grid columns to maintain alignment

	// Layout breakpoints
	WIDE_LAYOUT_MIN   = 120 // Minimum width for 4-column layout
	MEDIUM_LAYOUT_MIN = 80  // Minimum width for 2-column layout

	// Grid layout constants
	WIDE_COLUMNS   = 4 // 4-column grid for wide layout
	MEDIUM_COLUMNS = 2 // 2-column layout for medium screens
	NARROW_COLUMNS = 1 // Single column for narrow screens
)
